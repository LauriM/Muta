#ifndef AUTHENTICATION_H
#define AUTHENTICATION_H

class Authentication
{
private:
public:
	// todo: create account, etc, etc
	// todo: backend where the accounts are stored

	bool authenticate(String username, String password);
};

#endif